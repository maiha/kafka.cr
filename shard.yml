name: kafka
version: 0.6.4

authors:
  - maiha <maiha@wota.jp>

dependencies:
  msgpack:
    github: benoist/msgpack-crystal

development_dependencies:
  spec2:
    # github: waterlink/spec2.cr
    # TODO: restore to above repos when it supports 0.24
    github: maiha/spec2.cr
    branch: crystal-0.24

license: MIT
